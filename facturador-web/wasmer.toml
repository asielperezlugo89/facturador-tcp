[package]
name = "mayrelsuarezgomez/factura-tcp"
version = "1.0.8"
description = "Sistema de facturación para TCP Cuba"
repository = "https://github.com/asielperezlugo89/facturador-tcp"
readme = "README.md"

[dependencies]
"python/python" = "^0.2.0"

[fs]
"/app" = "."

[[command]]
name = "factura-tcp"
module = "python/python:python"
runner = "wasi"

[command.annotations.wasi]
main-args = [
    "-m",
    "flask",
    "--app",
    "/app/main",
    "run",
    "--host=0.0.0.0",
    "--port=8080",
    "--no-reload",
]
env = ["PYTHONEXECUTABLE=/bin/python", "PYTHONUNBUFFERED=1", "PYTHONPATH=/app/.venv-min/Lib/site-packages"]
